# Dependencias del proyecto Zigurat ERP — Agente Facturas
# Instalar con: pip install -r requirements.txt
# Versiones fijadas a las probadas en producción (2026-07-07). Actualizar a
# propósito (correr la suite después), nunca por accidente.

psycopg2-binary==2.9.11
pandas==2.3.3
openpyxl==3.1.5

# El agente del chat NO tiene dependencias: el loop es propio (contra
# OpenRouter) y las tools se declaran con app/agent/tools_base.py. Hasta el
# 2026-08-09 estaba `claude-agent-sdk`, usada solo por su decorador @tool — que
# marcaba TODOS los parámetros como obligatorios y obligaba al modelo a inventar
# filtros. Ver docs/superpowers/specs/2026-08-09-sacar-sdk-anthropic-design.md.

# Gráficos y export del dashboard
plotly==6.3.0
kaleido==0.2.1

# Tests (python -m pytest -q)
pytest==8.4.2
