# Credenciales
.env

# Archivos temporales del pipeline
changes.json
.changes_validated

# Python
__pycache__/
*.pyc

# Logs
logs/

# MCP config (contiene credenciales en la connection string)
.mcp.json

# Claude Code local settings
.claude/settings.local.json

# Datos crudos de entrada del pipeline (input transitorio, no se versiona)
facturas/
facturas-ventas/
transferencias/
facturas-compras/*.xml
Notas de Credito/*.xml
# Planilla de seguimiento manual de pagos (datos locales)
Pagos_Inv_Serv_Gas.xlsx

# Scratch del framework de skills (superpowers)
.superpowers/

# Artefactos de build de las cloud functions (deno bundle)
nube/dist/

# InsForge & AI agent skills (bloque agregado por el CLI de InsForge)
# OJO: .claude NO va aquí — en este repo .claude/ (skills, rules) SE VERSIONA.
.insforge
.agent
.agents
.augment
.cline
.github/copilot*
.kilocode
.qoder
.qwen
.roo
.trae
.windsurf
