# Credenciales
.env

# Archivos temporales del pipeline
changes.json
.changes_validated

# Python
__pycache__/
*.pyc

# Logs
logs/

# MCP config (contiene credenciales en la connection string)
.mcp.json

# Claude Code local settings
.claude/settings.local.json

# Datos crudos de entrada del pipeline (input transitorio, no se versiona)
facturas/
facturas-ventas/
transferencias/
facturas-compras/*.xml
Notas de Credito/*.xml
# Planilla de seguimiento manual de pagos (datos locales)
Pagos_Inv_Serv_Gas.xlsx

# Scratch del framework de skills (superpowers)
.superpowers/

# Repos de referencia clonados aparte (no son parte del proyecto)
github-repository/
